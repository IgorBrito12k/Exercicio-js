function resultado (dividendo, divisor) {
    let = result = Math.floor(dividendo / divisor)
    console.log(`Resultado da divisão é: ${result}`)
    console.log(`Resultado que sobrou é: ${dividendo % divisor}`)
}

resultado (10, 2)
resultado (10, 3)

